{
  "name": "jaga-id-technical-test",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "test:coverage": "turbo run test:coverage",
    "test:api": "npx newman run ./multi-tenant-hr-backend.postman_collection.json -e ./mthrb-local.postman_environment.json --env-var base_url=http://localhost --env-var company_service=company-a/api/v1 --env-var attendance_service=attendance/api/v1 --env-var companyId=A --reporters cli",
    "format": "turbo run format",
    "format:check": "turbo run format:check"
  },
  "devDependencies": {
    "@jaga-id/prettier-config": "workspace:*",
    "eslint-config-prettier": "^10.1.8",
    "turbo": "^2.1.2",
    "typescript": "^5.0.0"
  },
  "packageManager": "pnpm@9.0.0"
}
